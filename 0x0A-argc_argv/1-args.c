#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints num of args
 * @agrc: count the num of args
 * @argv: array of arfs
 */

int main(int argc, char* argv[])
{
	(void) argv;
	printf("%d\n", argc);
	return (0);
}
